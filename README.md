# crypto-portfolio

Portfolio tracker.

Open `index.html` in a browser (or serve it locally) to use the dashboard. The project now keeps a single entry point—`index.html`—so live data refreshes and API interactions run from one canonical page.
